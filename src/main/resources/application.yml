server:
  tomcat:
    mbeanregistry:
      enabled: true

spring:
  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    root: INFO

fcfs:
  event:
    name: CHICKEN
    limit: 100
  queue:
    admit:
      request: 8
      delay: 700
