spring:
  data:
    redis:
      host: localhost
      port: 6379

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    root: INFO

fcfs:
  event:
    name: CHICKEN
    limit: 1000
  queue:
    admit:
      request: 50
      delay: 500
